1. backspace img inside backspace btn 
    is not clickable to trigger onClick handler

2. checkLetters() does not discern if there is only one of a 
    particular letter, it will for example make multiple matches 
    yellow even if only one of that letter in the answer 

3. Fix favicon

4. bg color of keyboard

5. make letter tiles and keys bigger in mobile

6. keyboard bg colors should update with guesses 

7. clicking anywhere in the screen should not un focus 
    the current element

8. on delete, change tile border back to original light gray

9. make little Wordlr title appear in mobile to the left, small font